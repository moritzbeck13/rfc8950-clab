router id $ROUTER_LOOPBACK_ADDRESS;

filter accept_$CLIENT_LAN_NAME_filter {
    if net = $CLIENT_LAN_NETWORK/$CLIENT_LAN_PREFIX_LENGTH then {
        accept;
    }
    reject;
}

protocol device {
}

protocol direct {
    ipv4;
}

protocol kernel {
    ipv4 {
        import all;
        export all;
    };
}

template bgp $PEERING_LAN_NAME_template {
    local as $ASN;

    ipv4 {
        import all;
        export filter accept_$CLIENT_LAN_NAME_filter;

        extended next hop on;
    };
}

$NEIGHBORS